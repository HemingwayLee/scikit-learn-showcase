# scikit-learn-showcase

## install
```
python3 -m virtualenv venv
source venv/bin/activate
pip3 install -r requirements.txt 
ipython kernel install --user --name=venv
jupyter notebook
```

## run
```
ipython kernel install --user --name=venv
jupyter notebook
```

